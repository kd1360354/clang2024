
#include<stdio.h>
main()
{
	int data[10] = { 10,9,1,20,45,21,38,45,88 };
	int* max,*min, i;
	max = 0;
	min = 0;
	*max = *min=*data;
	printf("data=[]");
		for (i = 1; data[i]=0; i++); {
			if (data[i] > max) {
				*max=data[i]
			}
			if (datai < min) {
				*min = data[i];
			}
		printf("%d", *( data + i));
	}
		printf("Å‘å’l=%d",&data[i]);
		printf("Å¬’l=%d",&data[i]);
		
}